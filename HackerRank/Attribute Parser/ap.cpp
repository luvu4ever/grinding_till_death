#include<bits/stdc++.h>
#define f1(i,a) for(int i=1; i<=b; ++i)
#define f0(i,a) for(int i=0; i<a; ++i)
#define fi first
#define se second
#define pb push_back
#define mp make_pair

using namespace std;

int n, m;
string s[MAX_N];

main()
{
    // freopen("inp.txt", "r", stdin);
    // freopen("out.txt", "w", stdout);
    cin >> n >> q;
    f1(i, n)    getline(cin, s[i]);

    return 0;
}